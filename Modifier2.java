package pm5;
//import am.F  // F class 는 default로 작성이 되어 사용할 수 없다.
public class Modifier2 {

	public static void main(String[] args) {
		// class 의 접근 지정자
		/* 클래스는 public, default 접근 지정자만 사용할 수 이싸.
		 * 다시 말해 class 키워드 앞에는 public이 붙어 있거나 안붙어 있거나 둘 줄 하나이다.
		 * 
		 * default 클래스는 같은 패키지 내에서만 사용할 수 있고 public 클래스는 다른 패키지에서도
		 * 사용할 수 있다.
		 * 따라서 default 로 선언이 된다면 다른 패키지에서 import 가 불가능해진다.
		 */

	}

}
